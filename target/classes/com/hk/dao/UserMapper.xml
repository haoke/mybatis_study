<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hk.dao.UserMapper">

    <!-- Select use bind , in , concat -->
    <select id="findUserByNameAndIdInBindPassword" parameterType="user" resultType="user">
        select * from user
        <where>
            <include refid="query_user_where_id_in"/>
        </where>
    </select>

    <sql id="query_user_where_id_in">
        <bind name="password" value="'%' + password + '%'"/>
        <if test="username != null and username != ''">
            and username like concat('%', #{username},'%')
        </if>
        <if test="sex != null  and sex != ''">
            or sex like '%${sex}%'                                                <!-- TODO -->
        </if>
        or password like #{password}
        <foreach collection="ids" open="and id in (" close=")" item="id" separator=",">
            #{id}
        </foreach>
    </sql>

    <!-- Insert -->
    <insert id="insertUser" parameterType="user">
        <selectKey keyProperty="id" order="AFTER" resultType="int">
            select last_insert_id()
        </selectKey>
        insert into user(username, password) values (#{username},#{password})
    </insert>

    <!-- Update -->
    <update id="updateUser" parameterType="user">
        update user
        <trim>
            <set>
                <if test="username != null">
                    username = #{username},
                </if>
                <if test="password != null">
                    password = #{password},
                </if>
            </set>
        </trim>
        where id = #{id}
    </update>

    <!-- Delete -->
    <delete id="deleteUser" parameterType="int">
        delete         from user        where id = #{id}
    </delete>

    <!-- 使用实体类的包装对象作为查询条件-->
    <select id="findUserByNameFromUserQueryVo" parameterType="userQueryVo" resultType="user">
        select  * from user where username like #{user.username}
    </select>
</mapper>